---
description: The `kubevirt` builder is used to create a data volume using a virtual machine instance.
page_title: KubeVirt - Builders
nav_title: KubeVirt
---

# KubeVirt Builder

Type: `kubevirt`
Artifact BuilderId: `rohdealx.kubevirt`

The `kubevirt` builder is used to create a data volume using a virtual machine instance.

## Configuration Reference

### Required:

@include 'Config-required.mdx'

### Optional:

@include 'Config-not-required.mdx'
